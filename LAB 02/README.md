# program 1
📌 Program Description

This C++ program calculates the squared distance between two points in a two-dimensional plane. It uses the distance formula without taking the square root and then displays the result along with the coordinates of the two points. The program demonstrates basic arithmetic operations, variable usage, and formatted output in C++.

# program 2
📌 Program Description

This C++ program calculates the subtotal, sales tax, and total cost of five items. It stores item prices in variables, adds them to find the subtotal, applies a fixed 6% sales tax, and then displays the final total amount. The program demonstrates the use of variables, constants, arithmetic operations, and formatted output for basic billing calculations in C++.

# program 3
📌 Program Description

This C++ program calculates the average of three test scores and displays the result formatted to two decimal places. It demonstrates the use of arithmetic operations along with the <iomanip> library to control numeric output using fixed and setprecision.
